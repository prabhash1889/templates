# CP Templates Python 3
Competitive-programming snippets organized by topic. Copy only what you need into `main.py`.

Run normally with:
```bash
python3 main.py
```

Most snippets assume 0-indexed vertices and half-open ranges `[l, r)` unless the file says otherwise.
