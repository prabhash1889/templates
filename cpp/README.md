# CP Templates C++17
Advanced competitive-programming snippets organized by topic. Copy only what you need into `main.cpp`.
Compile normally with:
```bash
g++ -std=c++17 -O2 -pipe -Wall -Wextra -Wshadow main.cpp -o main
```
For debug macros, compile with `-DLOCAL`.
